二分查找在于对边界的掌控
1.while循环的边界更新一直持续到 l = =r;
2.做闭区间的更新移动 l = mid +1; r = mid -1
3.在循环的最后要明白如果该元素在数组中存在的题目，一定返回的是mid，但如果该数字不存在或者在整数之间，那么最后的状态一定是r = l - 1;这个时候如果向下取整就是r，插入元素就是l
4.找相同元素的上下边界的时候，考虑的是这个数组如果没有这个元素，或者比上下边界都大或小，这个时候l或者r是存在一个值的，但是它指向的不一定是边界， 需要第三个变量lb/rb来记录是否赋值，一定会有一个没有赋值的情况。
